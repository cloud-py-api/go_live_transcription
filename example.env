# AppAPI environment variables
APP_ID=live_transcription
APP_SECRET=12345
APP_VERSION=0.0.1
APP_PORT=23000
NEXTCLOUD_URL=http://nextcloud.appapi

# Live Transcription specific
LT_HPB_URL=wss://cloud.example.com/standalone-signaling/spreed
LT_INTERNAL_SECRET=your_hpb_internal_secret
SKIP_CERT_VERIFY=false

# Storage (Docker: auto-mounted, manual-install: set manually)
APP_PERSISTENT_STORAGE=persistent_storage

# Logging (set to "debug" for verbose output)
LT_LOG_LEVEL=info
